# 无人机任务参数配置
mission:
  # 视觉识别航点 [x, y] (相对起飞点)
  wp_recognition: [4.0, 0.0]

  # 空投航点 [x, y] (相对起飞点)
  wp_airdrop: [10.0, 5.0]

  # 打击/观测航点 [x, y] (相对起飞点)
  wp_strike: [15.0, -5.0]

  # 起飞高度 (米)
  takeoff_height: 0.6

# PCL 点云检测初始状态
pcl_enable:
  initial_enable: false
